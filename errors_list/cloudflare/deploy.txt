# Cloudflare 部署错误记录

## 2026-03-25 错误

**错误信息：**
```
ERROR Could not find compiled Open Next config, did you run the build command?
```

**根本原因：**
wrangler.jsonc 中的 build.command 设置为 "npm run build"，这只会运行 "next build"。
但部署到 Cloudflare Workers 需要运行 "opennextjs-cloudflare build" 来生成 .open-next/worker.js。

**解决方案：**
修改 wrangler.jsonc 中的 build.command 为 "npm run build:cloudflare"

**状态：** 已修复

## 2026-03-26 部署尝试 #1

**错误信息：**
```
./next.config.ts:44:3
Type error: Object literal may only specify known properties, but 'hideSourceMaps' does not exist in type 'SentryBuildOptions'. Did you mean to write 'sourcemaps'?
```

**根本原因：**
`@sentry/nextjs` 包的配置选项已更改。`hideSourceMaps` 和 `disableLogger` 选项已被弃用。

**解决方案：**
更新 Sentry 配置使用新的 `sourcemaps` 和 `bundleSizeOptimizations` 选项。

**状态：** 已修复

## 2026-03-26 部署尝试 #2

**错误信息：**
```
./next.config.ts:50:5
Type error: Object literal may only specify known properties, and 'excludeReplayCanvas' does not exist in type '{ excludeDebugStatements?: boolean | undefined; excludeTracing?: boolean | undefined; excludeReplayShadowDom?: boolean | undefined; excludeReplayIframe?: boolean | undefined; excludeReplayWorker?: boolean | undefined; }'.
```

**根本原因：**
`bundleSizeOptimizations.excludeReplayCanvas` 不是有效的选项。根据 TypeScript 类型定义，
有效的选项是：excludeDebugStatements, excludeTracing, excludeReplayShadowDom, excludeReplayIframe, excludeReplayWorker。

**解决方案：**
移除 `excludeReplayCanvas: true` 配置项。

**状态：** 已修复，但引发了新的错误

## 2026-03-26 部署尝试 #3

**错误信息：**
```
Error: This error should only happen for static 404 and 500 page from page router. File server/instrumentation.js does not exist
```

**根本原因：**
`instrumentation.ts` 文件与 Cloudflare Workers 不兼容。`@sentry/nextjs` 的 webpack 插件试图在构建时生成 instrumentation 代码，但 Cloudflare Workers 使用 OpenNext 适配器，有自己的入口点 `.open-next/worker.js`，不兼容 Next.js 的 instrumentation 钩子。

**解决方案：**
1. 完全移除 `instrumentation.ts` 文件
2. 移除所有 `sentry.*.config.ts` 文件 (client, edge, server)
3. 从 `next.config.ts` 中移除 `@sentry/nextjs` 的 `withSentryConfig` 包装器
4. 恢复为标准 Next.js 配置

**状态：** 已修复，已推送 GitHub 等待部署

## 2026-03-26 部署尝试 #4

**错误信息：**
```
✘ [ERROR] The `nodejs_compat` and `nodejs_compat_v2` compatibility flags cannot be used in together. Please select just one.
```

**根本原因：**
`wrangler.jsonc` 中同时配置了 `nodejs_compat` 和 `nodejs_compat_v2` 两个兼容性标志，这是不允许的。`nodejs_compat_v2` 是 v2 版本的 Node.js 兼容层，包含了 v1 的功能，所以不需要同时使用两者。

**解决方案：**
从 `wrangler.jsonc` 的 `compatibility_flags` 中移除 `nodejs_compat`，只保留 `nodejs_compat_v2`。

**状态：** 已修复，已推送 GitHub 等待部署
