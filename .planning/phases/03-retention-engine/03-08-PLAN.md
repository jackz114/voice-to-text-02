---
phase: 03-retention-engine
plan: 03-08
type: execute
wave: 1
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05", "03-06", "03-07"]
files_modified:
  - scripts/test-integration.ts
  - scripts/test-search-e2e.ts
  - scripts/test-email-flow.ts
  - .planning/phases/03-retention-engine/03-08-VERIFICATION.md
autonomous: true
requirements:
  - NOTIFY-01
  - NOTIFY-02
  - NOTIFY-03
  - NOTIFY-04
  - SEARCH-01
  - SEARCH-02
  - SEARCH-03
must_haves:
  truths:
    - "Search works end-to-end: UI input → API call → Database query → Results display"
    - "Email notification flow works: Cron trigger → Preferences check → Email render → Send"
    - "User preferences affect search (history on/off) and notifications (time, domains, enabled)"
    - "All SEARCH requirements (01/02/03) are satisfied by the implemented system"
    - "All NOTIFY requirements (01/02/03/04) are satisfied by the implemented system"
  artifacts:
    - path: "scripts/test-integration.ts"
      provides: "End-to-end integration test suite"
      min_lines: 100
    - path: "scripts/test-search-e2e.ts"
      provides: "Search-specific E2E test scenarios"
      min_lines: 80
    - path: "scripts/test-email-flow.ts"
      provides: "Email notification flow test scenarios"
      min_lines: 80
    - path: ".planning/phases/03-retention-engine/03-08-VERIFICATION.md"
      provides: "Integration test results and verification report"
      min_lines: 50
  key_links:
    - from: "SearchModal.tsx"
      to: "/api/search"
      via: "fetch with debounce"
      pattern: "fetch\(.*\/api\/search"
    - from: "/api/search"
      to: "knowledge_items.search_vector"
      via: "Drizzle ORM query with tsvector"
      pattern: "search_vector @@"
    - from: "Cron Trigger"
      to: "/api/cron/daily-email"
      via: "HTTP POST with CRON_SECRET"
      pattern: "Authorization: Bearer"
    - from: "daily-email route"
      to: "user_preferences table"
      via: "Drizzle query for notification settings"
      pattern: "emailNotificationsEnabled"
    - from: "NotificationPreferences.tsx"
      to: "/api/notifications/preferences"
      via: "POST with preference updates"
      pattern: "fetch\(.*\/api\/notifications\/preferences"
---

<objective>
Create comprehensive end-to-end integration tests that verify all Phase 3 (Retention Engine) components work together correctly.

Purpose: Ensure the search functionality, email notifications, and user preferences systems are fully integrated and satisfy all requirements before marking Phase 3 complete.

Output: Integration test scripts and verification report documenting that SEARCH-01/02/03 and NOTIFY-01/02/03/04 requirements are met.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-retention-engine/03-CONTEXT.md
@.planning/phases/03-retention-engine/03-UAT.md

## Key Interfaces from Completed Plans

### Search System (03-02, 03-03, 03-04)

From `src/lib/search.ts`:
```typescript
export interface SearchResult {
  id: string;
  title: string;
  content: string;
  excerpt: string;
  domain: string;
  tags: string[];
  source: string | null;
  createdAt: string;
  rank: number;
}

export interface SearchResponse {
  results: SearchResult[];
  query: string;
  total: number;
  hasMore: boolean;
}
```

API Endpoint: `GET /api/search?q={query}&domain={domain}&limit={limit}&offset={offset}`

### User Preferences (03-01, 03-07)

From `src/db/schema.ts`:
```typescript
export interface UserPreferences {
  userId: string;
  emailNotificationsEnabled: boolean;
  dailyReminderTime: string; // HH:mm format
  reminderTimezone: string;
  includedDomains: string[];
  saveSearchHistory: boolean;
  displayName: string | null;
}
```

API Endpoints:
- `GET /api/notifications/preferences` - Returns UserPreferences
- `POST /api/notifications/preferences` - Updates preferences

### Email System (03-05, 03-06)

From `src/lib/email-templates.ts`:
```typescript
interface DailyReminderEmailData {
  username: string;
  count: number;
  domains: string[];
  dueDate: string;
}
```

Cron Endpoint: `POST /api/cron/daily-email` (requires `Authorization: Bearer {CRON_SECRET}`)

### Search History Hook (03-03)

From `src/hooks/useSearchHistory.ts`:
```typescript
interface UseSearchHistoryReturn {
  history: string[];
  addToHistory: (query: string) => void;
  removeFromHistory: (query: string) => void;
  clearHistory: () => void;
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Search End-to-End Test Script</name>
  <files>scripts/test-search-e2e.ts</files>
  <read_first>
    - src/lib/search.ts
    - src/app/api/search/route.ts
    - src/components/search/SearchModal.tsx
    - src/hooks/useSearchHistory.ts
  </read_first>
  <action>
Create a TypeScript test script at `scripts/test-search-e2e.ts` that validates the complete search flow:

1. **Test Data Setup**: Define mock search queries and expected result structures

2. **API Contract Tests**:
   - Verify `/api/search` accepts query parameters: q (string, 2-100 chars), domain (optional string), limit (number 1-20, default 10), offset (number, default 0)
   - Verify response matches SearchResponse interface with results array, query string, total count, hasMore boolean
   - Verify each result has: id (uuid), title (string), content (string), excerpt (string with &lt;mark&gt; tags), domain (string), tags (string array), source (string or null), createdAt (ISO date), rank (number)

3. **Search Flow Integration Tests**:
   - Test query validation: reject queries under 2 characters, reject queries over 100 characters
   - Test debounce behavior: verify search API is not called more than once per 300ms
   - Test search history: verify localStorage key `search-history` stores max 8 items
   - Test domain filtering: verify domain parameter filters results to matching domain
   - Test result ranking: verify results ordered by rank descending (highest first)
   - Test highlighted excerpts: verify &lt;mark&gt; tags wrap matching terms in excerpt field

4. **Error Handling Tests**:
   - Verify 401 response for unauthenticated requests
   - Verify 400 response for invalid query parameters
   - Verify 200 with empty results array for queries with no matches

5. **Output**: Console log test results with PASS/FAIL status for each test case
  </action>
  <verify>
    <automated>grep -n "test-search-e2e" scripts/test-search-e2e.ts && npx tsx scripts/test-search-e2e.ts 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/test-search-e2e.ts` exists with TypeScript syntax
    - Script contains at least 8 test cases covering API contract, flow integration, and error handling
    - Script imports types from `src/lib/search.ts` for type safety
    - Script outputs structured test results to console
    - Script references the exact API endpoint path `/api/search`
    - Script validates response contains `results`, `query`, `total`, `hasMore` fields
  </acceptance_criteria>
  <done>Search E2E test script created with comprehensive test coverage for API contract, debounce, history, filtering, ranking, and error handling</done>
</task>

<task type="auto">
  <name>Task 2: Create Email Notification Flow Test Script</name>
  <files>scripts/test-email-flow.ts</files>
  <read_first>
    - src/app/api/cron/daily-email/route.ts
    - src/lib/email.ts
    - src/lib/email-templates.ts
    - src/components/notifications/DailyReminderEmail.tsx
  </read_first>
  <action>
Create a TypeScript test script at `scripts/test-email-flow.ts` that validates the complete email notification flow:

1. **Cron Trigger Tests**:
   - Verify cron endpoint `POST /api/cron/daily-email` requires `Authorization: Bearer` header
   - Verify endpoint returns 401 without valid CRON_SECRET
   - Verify endpoint accepts POST requests only

2. **User Preference Filtering Tests**:
   - Verify users with `emailNotificationsEnabled: false` are skipped
   - Verify timezone conversion: user local time maps to correct UTC hour for cron check
   - Verify `dailyReminderTime` (HH:mm format) is parsed correctly
   - Verify `includedDomains` filtering: only count due items from included domains
   - Verify empty `includedDomains` array means "all domains"

3. **Due Items Query Tests**:
   - Verify query joins `review_state` with `knowledge_items` table
   - Verify items with `next_review_at <= now()` are considered due
   - Verify items are grouped by user and domain for counting

4. **Email Content Tests**:
   - Verify `renderDailyReminderEmail` generates HTML with inline CSS
   - Verify email includes: personalized greeting with username, count of due items, domain badges list, CTA button linking to `/review?session=daily&source=email`
   - Verify urgency color coding: red for 10+ items, amber for 5-9, blue for <5
   - Verify plain text version is generated alongside HTML
   - Verify email subject format: "今日有 X 个知识点待复习"

5. **Batch Processing Tests**:
   - Verify emails are sent sequentially (not parallel) to avoid rate limits
   - Verify per-user failures don't block other users
   - Verify summary returned with sent/failed/skipped counts

6. **Manual Trigger Tests**:
   - Verify `POST /api/notifications/send-daily` requires ADMIN_SECRET
   - Verify manual trigger forwards to cron handler correctly

7. **Output**: Console log test results with PASS/FAIL status for each test case
  </action>
  <verify>
    <automated>grep -n "test-email-flow" scripts/test-email-flow.ts && npx tsx scripts/test-email-flow.ts 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/test-email-flow.ts` exists with TypeScript syntax
    - Script contains at least 10 test cases covering cron trigger, preferences, due items, email content, and batch processing
    - Script references exact environment variables: CRON_SECRET, ADMIN_SECRET, RESEND_API_KEY
    - Script validates email template includes required elements: greeting, count, domains, CTA button
    - Script validates timezone handling with major timezones (Asia/Shanghai, America/New_York, Europe/London)
    - Script validates the exact deep link URL pattern: `/review?session=daily&source=email`
  </acceptance_criteria>
  <done>Email flow test script created with comprehensive coverage for cron trigger, preference filtering, email content, and batch processing</done>
</task>

<task type="auto">
  <name>Task 3: Create Integration Test Suite and Verification Report</name>
  <files>scripts/test-integration.ts, .planning/phases/03-retention-engine/03-08-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/03-retention-engine/03-UAT.md
    - .planning/REQUIREMENTS.md
    - src/db/schema.ts
  </read_first>
  <action>
Create two files:

**File 1: `scripts/test-integration.ts`**

A master integration test script that orchestrates and runs all E2E tests:

1. **Test Runner Framework**:
   - Import and run test cases from `test-search-e2e.ts` and `test-email-flow.ts`
   - Implement test runner with before/after hooks
   - Track total tests, passed, failed counts
   - Generate structured JSON test report

2. **Cross-Cutting Integration Tests**:
   - Test: User changes notification preferences → preferences persist to database → cron job reads updated preferences
   - Test: User disables search history → localStorage not written → history remains empty
   - Test: User disables email notifications → cron skips user → no email sent even with due items
   - Test: User filters domains → search results respect filter → email counts respect filter
   - Test: Search query with no results → empty state displays → "create new note" button works

3. **Requirement Verification Matrix**:
   - SEARCH-01: Natural language search works via `/api/search` with tsvector
   - SEARCH-02: Results sorted by ts_rank relevance score
   - SEARCH-03: Results show preview (excerpt) and source
   - NOTIFY-01: Cron runs hourly, users receive emails at preferred local time
   - NOTIFY-02: Only sends when due items exist, respects emailNotificationsEnabled
   - NOTIFY-03: Email includes count and domain list
   - NOTIFY-04: Email sent via Resend API with proper formatting

4. **Exit Codes**:
   - Exit 0 if all tests pass
   - Exit 1 if any test fails

**File 2: `.planning/phases/03-retention-engine/03-08-VERIFICATION.md`**

Create a verification report documenting:

1. **Test Summary Table**:
   | Test Suite | Tests | Passed | Failed |
   |------------|-------|--------|--------|
   | Search E2E | X | X | X |
   | Email Flow | X | X | X |
   | Integration | X | X | X |

2. **Requirement Coverage**:
   | Requirement | Test Evidence | Status |
   |-------------|---------------|--------|
   | SEARCH-01 | Link to test case | PASS |
   | SEARCH-02 | Link to test case | PASS |
   | etc. |

3. **Integration Points Verified**:
   - SearchModal → /api/search → knowledge_items.search_vector
   - Cron Trigger → /api/cron/daily-email → user_preferences → Resend
   - NotificationPreferences → /api/notifications/preferences → user_preferences table

4. **Known Limitations** (if any):
   - Tests that require actual database/Resend API are mocked
   - Manual verification steps documented

5. **Sign-off Section**:
   - "Phase 3 Retention Engine integration testing complete"
   - Date and commit hash
  </action>
  <verify>
    <automated>npx tsx scripts/test-integration.ts 2>&1 | tail -20 && ls -la .planning/phases/03-retention-engine/03-08-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/test-integration.ts` exists and can be executed with `npx tsx`
    - Script imports and runs tests from the other two test files
    - Script generates structured output with test counts and PASS/FAIL status
    - Script exits with code 0 on success, code 1 on failure
    - File `.planning/phases/03-retention-engine/03-08-VERIFICATION.md` exists
    - Verification document includes requirement coverage table for all 7 requirements (SEARCH-01/02/03, NOTIFY-01/02/03/04)
    - Verification document includes integration points section with specific file/function references
    - Verification document includes test summary with counts
  </acceptance_criteria>
  <done>Master integration test suite and verification report created, documenting that all Phase 3 requirements are satisfied</done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Run the integration test suite:
   ```bash
   npx tsx scripts/test-integration.ts
   ```

2. Verify all test files exist:
   ```bash
   ls -la scripts/test-*.ts
   ```

3. Verify verification report exists:
   ```bash
   cat .planning/phases/03-retention-engine/03-08-VERIFICATION.md
   ```

4. Check that all 7 requirements are covered in the verification report:
   - SEARCH-01, SEARCH-02, SEARCH-03
   - NOTIFY-01, NOTIFY-02, NOTIFY-03, NOTIFY-04
</verification>

<success_criteria>
1. All three test scripts exist and are executable
2. Integration test suite runs without errors
3. Verification report documents coverage of all 7 Phase 3 requirements
4. Test scripts validate key integration points between components
5. Report includes specific evidence linking tests to requirements
</success_criteria>

<output>
After completion, create `.planning/phases/03-retention-engine/03-08-SUMMARY.md` documenting:
- Integration test coverage summary
- Requirement verification status
- Any integration issues discovered
- Phase 3 completion sign-off
</output>
