---
phase: 02-review-loop
plan: "06"
type: execute
wave: 3
depends_on: ["02-02", "02-04", "02-05"]
files_modified: []
autonomous: false
requirements: [LIB-01, LIB-02, LIB-03, FSRS-01, FSRS-02, FSRS-03, FSRS-04, REVIEW-01, REVIEW-02, REVIEW-03, REVIEW-04, AUDIO-01, AUDIO-02, AUDIO-03, TRANS-01, TRANS-02, TRANS-03]

must_haves:
  truths:
    - "All 7 phase success criteria are verifiable by a human user"
    - "No regression in Phase 1 capture and confirmation flow"
  artifacts:
    - path: ".planning/phases/02-review-loop/02-06-SUMMARY.md"
      provides: "Human verification sign-off record"
  key_links:
    - from: "/library"
      to: "/api/library/list"
      via: "domain filter sidebar"
    - from: "/review"
      to: "/api/review/rate"
      via: "FSRS rating submission"
    - from: "/capture AudioRecorder"
      to: "Supabase Storage audio bucket"
      via: "signed URL direct upload"
---

<objective>
Human verification of all Phase 2 capabilities before marking the phase complete.

Purpose: Confirms all 7 phase success criteria are met by a real user testing the actual application.
Output: Human sign-off. No code changes expected.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-review-loop/02-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 2: Review Loop implementation including:
    - Knowledge Library at /library (domain sidebar + list/grid view + item detail modal + delete)
    - Review Session at /review (swipeable card stack + click-to-reveal + FSRS rating + undo bar)
    - Audio Recorder on /capture page (MediaRecorder + Supabase Storage direct upload + Whisper transcription)
    - Two library API routes, two review API routes, two audio API routes
    - ts-fsrs and framer-motion installed
  </what-built>
  <how-to-verify>
    Complete all steps below. The app runs at http://localhost:3000 (start with `npm run dev`).

    **Prerequisites (setup required once):**
    - Database is migrated (`npx drizzle-kit migrate` or schema applied to Supabase)
    - Supabase `audio` bucket exists (private) with INSERT and SELECT RLS policies (see 02-05-PLAN.md user_setup)
    - At least 2-3 knowledge items exist in the database (create them via /capture if needed)
    - At least one knowledge item has nextReviewAt set to today or earlier

    **Criterion 1 — Knowledge Library Browse (LIB-01, LIB-02):**
    1. Log in and visit http://localhost:3000/library
    2. Verify: left sidebar shows "全部" and domain names with item counts
    3. Click a specific domain in the sidebar
    4. Verify: main content area updates to show only items from that domain
    5. Click any item card
    6. Verify: a modal opens showing the FULL content of that item (not just the 50-char preview)
    7. Close the modal

    **Criterion 2 — Knowledge Item Delete (LIB-03):**
    1. On /library, hover over (or swipe left on) a knowledge item
    2. Click the delete button
    3. Confirm the deletion dialog
    4. Verify: the item disappears from the list without a full page reload

    **Criterion 3 — FSRS Initial Scheduling (FSRS-01, FSRS-02):**
    1. Go to /capture and create a new knowledge item (text paste + confirm)
    2. Check the database (Supabase Dashboard -> Table Editor -> review_state)
    3. Verify: the new row has nextReviewAt = tomorrow (not today, not far future)
    4. Verify: stability = 0 and difficulty = 0 (Phase 1 placeholder, will update on first review)

    **Criterion 4 — Today's Review Queue (REVIEW-01, REVIEW-02):**
    1. Visit http://localhost:3000/review
    2. Verify: page shows cards for items with nextReviewAt <= today
    3. Verify: each card initially shows ONLY the title (content hidden)
    4. Click a card
    5. Verify: full content appears below the title

    **Criterion 5 — FSRS Rating and Update (REVIEW-03, REVIEW-04, FSRS-03, FSRS-04):**
    1. On /review, reveal a card and click the "🙂 准确回忆 (Good)" button
    2. Verify: that card disappears from the stack
    3. Verify: an undo bar appears at the top right reading "刚才评错了？"
    4. Check the database: review_state row for that item should have reviewCount = 1 (previously 0), nextReviewAt > today, stability > 0
    5. Rate 5 items in a row with "🚀 秒答且轻松 (Easy)"
    6. Verify: after the 5th Easy rating, a nudge appears briefly: "真的这么轻松吗？如果下次忘了，进度会重置哦。"
    7. After completing all due items: verify completion screen shows "今日复习完成！"

    **Criterion 6 — Audio Direct Upload (AUDIO-01, AUDIO-02, AUDIO-03):**
    1. Visit http://localhost:3000/capture while logged in
    2. Verify: page shows BOTH a text paste area AND an audio recorder section side by side
    3. Click "🎙️ 开始录制" and allow microphone access
    4. Verify: waveform animation appears and elapsed time increments
    5. Pause and resume recording (verify pause/resume works)
    6. Click "⏹ 停止并转写"
    7. Verify: "正在上传..." message appears, then "正在转写..."
    8. Check Cloudflare Workers logs (`npm run cf:logs`) — verify NO large binary payload appears (audio went directly to Supabase)
    9. Check Supabase Storage dashboard — verify audio file appears under {userId}/ folder

    **Criterion 7 — Whisper Transcription (TRANS-01, TRANS-02, TRANS-03):**
    1. After the recording stops and transcribes (from Criterion 6)
    2. Verify: the text area on /capture is auto-populated with the transcribed text
    3. Check Supabase Table Editor -> transcriptions table
    4. Verify: a new row exists with status = "completed", text containing the transcription, userId matching your user, audioUrl matching the storage path

    **Regression check:**
    1. Complete the full capture flow: paste text → extract → review cards → confirm
    2. Verify: Phase 1 behavior is unchanged
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 7 criteria pass, or describe which criteria failed and what behavior you observed.
  </resume-signal>
</task>

</tasks>

<verification>
This plan IS the verification. Human sign-off required.
</verification>

<success_criteria>
Human has typed "approved" after verifying all 7 Phase 2 success criteria against the running application.
</success_criteria>

<output>
After approval, create `.planning/phases/02-review-loop/02-06-SUMMARY.md` with verification results.
Then update `.planning/STATE.md` to mark Phase 2 as complete.
</output>
