---
phase: 02-review-loop
task: 2
total_tasks: 2
status: paused
last_updated: 2026-03-23T21:00:00+08:00
---

<current_state>
Phase 02 (review-loop) gap closure complete. Two Wave 2 tasks finished:

1. **02-02-GAP**: Domain sidebar (already correct) + grid card delete button fix - committed
2. **02-04-GAP**: Browse mode for proactive review - committed

**Wave 3 pending**: 02-02-GAP-UX (add "去复习" navigation to knowledge detail modal)

All Wave 2 gap fixes are committed. No uncommitted changes.
</current_state>

<completed_work>

- **Task 1: 02-02-GAP** - Library UX fixes - Done (commit: c600801)
  - Grid card delete button now visible on hover (added `group` class)
  - DomainSidebar was already correctly implemented

- **Task 2: 02-04-GAP** - Browse mode for proactive review - Done (commit: b6c7075)
  - Added `ReviewMode` type: `"scheduled" | "browse"`
  - Added empty state UI with "📚 主动复习（浏览全部）" button
  - Implemented browse mode fetching from `/api/library/list`
  - Added simplified `BrowseCard` without FSRS rating buttons
  - Added browse mode indicator banner with "返回今日复习" button
  - Updated `/api/library/list` to include `content` field for browse mode

</completed_work>

<remaining_work>

- **Wave 3: 02-02-GAP-UX** - Add review navigation to detail modal - Not started
  - Add "🔄 去复习" button linking to `/review` in knowledge detail modal
  - Add "✅ 标记为已复习" button to mark item as reviewed
  - File: `src/components/library/KnowledgeLibrary.tsx`

</remaining_work>

<decisions_made>

- Browse mode uses simplified card UI without swipe gestures or FSRS ratings
- Browse mode indicator banner placed above card stack with return button
- Empty state shows friendly "今日复习已完成！" message with two action buttons
- Browse mode completion state allows restart or return to scheduled mode

</decisions_made>

<blockers>

None. Ready to proceed with Wave 3 (02-02-GAP-UX).

</blockers>

<context>
Phase 02 review-loop is nearly complete. Core functionality is all implemented:
- Library UI with domain filter, list/grid toggle, detail modal
- Review session with FSRS scheduling, card stack UI, swipe gestures
- Audio capture with WebM→WAV conversion
- Browse mode for proactive review without FSRS scheduling

Remaining work is a small UX enhancement (Wave 3) to add navigation from detail modal to review flow.
</context>

<next_action>
Read `.planning/phases/02-review-loop/02-02-GAP-UX-PLAN.md` then implement the review navigation buttons in the knowledge detail modal.
</next_action>
