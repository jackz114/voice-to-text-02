---
phase: 02-review-loop
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/library/page.tsx
  - src/components/library/KnowledgeLibrary.tsx
  - src/components/library/DomainSidebar.tsx
  - src/components/library/KnowledgeItemCard.tsx
autonomous: true
requirements: [LIB-01, LIB-02, LIB-03]

must_haves:
  truths:
    - "Visiting /library shows a page with a left sidebar listing all domains the user has items in, plus an '全部' (All) option"
    - "Clicking a domain in the sidebar filters the main content to show only items from that domain"
    - "The main content shows items in list view by default, with a toggle to switch to grid view"
    - "Each item card displays: title, domain badge, next review date, and first 50 chars of content"
    - "Web users see action buttons on hover; mobile users can swipe left to reveal delete action"
    - "Clicking an item opens a detail view showing full content (either modal or new page)"
    - "Delete action removes the item and updates the list without page reload"
  artifacts:
    - path: "src/app/library/page.tsx"
      provides: "Library page shell with auth guard"
      exports: ["default"]
    - path: "src/components/library/KnowledgeLibrary.tsx"
      provides: "Main library shell: sidebar + content area"
      exports: ["KnowledgeLibrary"]
    - path: "src/components/library/DomainSidebar.tsx"
      provides: "Left sidebar with domain list"
      exports: ["DomainSidebar"]
    - path: "src/components/library/KnowledgeItemCard.tsx"
      provides: "Individual item card (list/grid variants)"
      exports: ["KnowledgeItemCard"]
  key_links:
    - from: "src/components/library/KnowledgeLibrary.tsx"
      to: "/api/library/list"
      via: "fetch with Bearer token"
      pattern: "fetch.*api/library/list"
    - from: "src/components/library/KnowledgeItemCard.tsx"
      to: "/api/library/delete"
      via: "DELETE fetch on delete action"
      pattern: "fetch.*api/library/delete"
    - from: "src/components/library/DomainSidebar.tsx"
      to: "src/components/library/KnowledgeLibrary.tsx"
      via: "onDomainSelect callback prop"
      pattern: "onDomainSelect={.*}"
---

<objective>
Build the complete knowledge library UI: a sidebar for domain navigation, a list/grid toggleable view of knowledge items, and per-item actions (view detail, delete).

Purpose: Implements LIB-01, LIB-02, LIB-03 requirements. Depends on 02-01 API routes.
Output: Library page at /library with full browsing and deletion capability.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-review-loop/02-CONTEXT.md
@.planning/phases/02-review-loop/02-RESEARCH.md

<interfaces>
<!-- API contract from 02-01 plan -->
GET /api/library/list returns:
```typescript
{
  items: Array<{
    id: string;
    title: string;
    domain: string;
    source: string | null;
    tags: string[];
    createdAt: string;
    nextReviewAt: string;
    reviewCount: number;
    contentPreview: string; // first 50 chars
  }>
}
```

DELETE /api/library/delete body:
```typescript
{ id: string }
```

<!-- Existing auth hook -->
From src/components/auth/AuthProvider.tsx:
```typescript
export function useAuth(): { user: User | null; loading: boolean; signOut: () => void; refreshUser: () => void }
```

<!-- Existing UI patterns from ConfirmationCards.tsx -->
Card styling pattern: Tailwind classes like `border rounded-lg p-4 shadow-sm hover:shadow-md transition-shadow`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create KnowledgeLibrary component shell</name>
  <files>src/components/library/KnowledgeLibrary.tsx</files>
  <read_first>
    - src/components/auth/AuthProvider.tsx (useAuth hook pattern)
    - src/components/capture/ConfirmationCards.tsx (card styling patterns)
    - src/db/schema.ts (domain field definition)
  </read_first>
  <action>
Create `src/components/library/KnowledgeLibrary.tsx` as a client component (`"use client"` at top).

The component must:

1. Import and use `useAuth()` from `@/components/auth/AuthProvider` to get the current user and access token.

2. Define TypeScript interface for the item type returned by the API:
   ```typescript
   interface KnowledgeItem {
     id: string;
     title: string;
     domain: string;
     source: string | null;
     tags: string[];
     createdAt: string;
     nextReviewAt: string;
     reviewCount: number;
     contentPreview: string;
   }
   ```

3. Maintain state:
   - `items: KnowledgeItem[]` — loaded items
   - `loading: boolean` — fetch state
   - `error: string | null` — error message
   - `selectedDomain: string | null` — null means "全部" (All)
   - `viewMode: 'list' | 'grid'` — toggle state

4. Implement `fetchItems(domain?: string)` async function:
   - Build URL: `/api/library/list${domain ? `?domain=${encodeURIComponent(domain)}` : ''}`
   - Include `Authorization: Bearer ${accessToken}` header from `useAuth()`
   - On 401: set error to "请先登录"
   - On success: set items to response.items
   - On error: set error to "加载失败，请重试"

5. Use `useEffect` to call `fetchItems(selectedDomain ?? undefined)` when `selectedDomain` changes.

6. Render layout:
   - Container: `div className="flex h-full min-h-[calc(100vh-4rem)]"`
   - Left sidebar area: `aside className="w-64 border-r p-4 hidden md:block"` — placeholder for DomainSidebar
   - Main content: `main className="flex-1 p-4"`

7. In main content:
   - Header row with: title "知识库", view mode toggle buttons (列表/卡片), item count
   - If loading: show spinner or "加载中..."
   - If error: show error message in red
   - If items empty: show "暂无知识条目，去添加一些吧" with link to /capture
   - Otherwise: render grid of KnowledgeItemCard components

Import paths:
```typescript
"use client";
import { useState, useEffect } from "react";
import { useAuth } from "@/components/auth/AuthProvider";
```
  </action>
  <verify>
    <automated>cd E:/projects_all/voice-to-text/voice-to-txt-02 && npm run lint -- --max-warnings=0 2>&1 | grep -E "(error|warning)" | grep "KnowledgeLibrary" || echo "lint OK"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/components/library/KnowledgeLibrary.tsx
    - File starts with `"use client"`
    - File contains `interface KnowledgeItem`
    - File contains `useAuth()` import and usage
    - File contains `fetchItems` function with Bearer token header
    - File contains `selectedDomain` state
    - File contains `viewMode` state with type `'list' | 'grid'`
    - File contains `/api/library/list` fetch call
  </acceptance_criteria>
  <done>KnowledgeLibrary component shell exists with state management, API integration, and layout structure</done>
</task>

<task type="auto">
  <name>Task 2: Create DomainSidebar component</name>
  <files>src/components/library/DomainSidebar.tsx</files>
  <read_first>
    - src/components/library/KnowledgeLibrary.tsx (interface and callback expectations)
  </read_first>
  <action>
Create `src/components/library/DomainSidebar.tsx` as a client component.

Props interface:
```typescript
interface DomainSidebarProps {
  domains: string[]; // unique list of domains from all items
  selectedDomain: string | null; // null = All
  onSelect: (domain: string | null) => void;
  itemCounts: Record<string, number>; // domain -> count mapping
}
```

The component must:

1. Render a sidebar with:
   - Header: "领域" (Domains)
   - "全部" (All) option at top, always visible, highlighted when `selectedDomain === null`
   - List of domain buttons below, each showing domain name and count badge

2. Styling (Tailwind):
   - Container: `div className="space-y-1"`
   - Each item: `button` with `className="w-full flex items-center justify-between px-3 py-2 rounded-md text-sm transition-colors"`
   - Selected state: `bg-primary text-primary-foreground` (or `bg-blue-100 text-blue-900` if primary vars unavailable)
   - Unselected state: `hover:bg-gray-100 text-gray-700`
   - Count badge: `span className="ml-2 text-xs bg-gray-200 text-gray-600 px-2 py-0.5 rounded-full"`

3. Clicking a domain button calls `onSelect(domain)` (or `onSelect(null)` for "全部").

4. The component is purely presentational — no data fetching.

Import paths:
```typescript
"use client";
```
  </action>
  <verify>
    <automated>cd E:/projects_all/voice-to-text/voice-to-txt-02 && npm run lint -- --max-warnings=0 2>&1 | grep -E "(error|warning)" | grep "DomainSidebar" || echo "lint OK"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/components/library/DomainSidebar.tsx
    - File contains `interface DomainSidebarProps`
    - File contains `domains`, `selectedDomain`, `onSelect`, `itemCounts` props
    - File contains "全部" button
    - File contains domain count badge rendering
    - File uses Tailwind classes for selected/unselected states
  </acceptance_criteria>
  <done>DomainSidebar component exists with domain list, selection handling, and count badges</done>
</task>

<task type="auto">
  <name>Task 3: Create KnowledgeItemCard component</name>
  <files>src/components/library/KnowledgeItemCard.tsx</files>
  <read_first>
    - src/components/capture/ConfirmationCards.tsx (card styling and button patterns)
    - src/components/library/KnowledgeLibrary.tsx (KnowledgeItem interface)
  </read_first>
  <action>
Create `src/components/library/KnowledgeItemCard.tsx` as a client component.

Props interface:
```typescript
interface KnowledgeItemCardProps {
  item: KnowledgeItem; // reuse the interface from KnowledgeLibrary
  viewMode: 'list' | 'grid';
  onDelete: (id: string) => void;
  onClick: (item: KnowledgeItem) => void;
}
```

The component must:

1. Render different layouts based on `viewMode`:

   **List mode** (`viewMode === 'list'`):
   - Container: `div className="flex items-center justify-between p-4 border rounded-lg hover:shadow-sm transition-shadow cursor-pointer"`
   - Left side: Title (bold), domain badge, content preview (text-gray-600 truncate)
   - Right side: Next review date (formatted), review count badge
   - Hover actions (Web): show delete button on hover

   **Grid mode** (`viewMode === 'grid'`):
   - Container: `div className="p-4 border rounded-lg hover:shadow-sm transition-shadow cursor-pointer"`
   - Top: Domain badge (small)
   - Middle: Title (bold, larger), content preview (line-clamp-2)
   - Bottom: Next review date, review count

2. Domain badge styling: `span className="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-blue-100 text-blue-800"`

3. Content preview: truncate to 50 chars (already provided by API as contentPreview)

4. Date formatting: use `new Date(item.nextReviewAt).toLocaleDateString('zh-CN')` to show "2026/03/24"

5. Clicking the card (not the delete button) calls `onClick(item)`.

6. Delete button:
   - Only visible on hover in list mode, or always visible in a corner for grid mode
   - Calls `onDelete(item.id)` with confirmation: `confirm('确定要删除这条知识吗？')`
   - Styling: `button className="text-red-600 hover:text-red-800 text-sm"` with trash icon or "删除" text

Import paths:
```typescript
"use client";
```

Note: The KnowledgeItem type should be imported or redefined here. If importing creates circular dependency, redefine the interface locally with identical shape.
  </action>
  <verify>
    <automated>cd E:/projects_all/voice-to-text/voice-to-txt-02 && npm run lint -- --max-warnings=0 2>&1 | grep -E "(error|warning)" | grep "KnowledgeItemCard" || echo "lint OK"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/components/library/KnowledgeItemCard.tsx
    - File contains `interface KnowledgeItemCardProps`
    - File contains `viewMode` prop with type `'list' | 'grid'`
    - File contains conditional rendering based on viewMode
    - File contains `onDelete` prop and calls it with confirmation
    - File contains `onClick` prop
    - File contains domain badge styling
    - File contains date formatting with toLocaleDateString
  </acceptance_criteria>
  <done>KnowledgeItemCard component exists with list/grid variants, delete confirmation, and click handling</done>
</task>

<task type="auto">
  <name>Task 4: Wire components and create /library page</name>
  <files>src/app/library/page.tsx</files>
  <read_first>
    - src/components/library/KnowledgeLibrary.tsx
    - src/components/library/DomainSidebar.tsx
    - src/components/library/KnowledgeItemCard.tsx
    - src/app/capture/page.tsx (page structure pattern)
  </read_first>
  <action>
Create `src/app/library/page.tsx` as the library page.

The page must:

1. Be a client component (`"use client"`) because it uses hooks.

2. Import and render `KnowledgeLibrary` component (which contains the main logic).

3. Add auth guard: if `useAuth()` returns `loading`, show "加载中..."; if `user` is null, show "请先登录" with a link to /login.

4. The KnowledgeLibrary component should be updated to:
   - Import and render `DomainSidebar` in the sidebar area, passing:
     - `domains`: computed from `Array.from(new Set(items.map(i => i.domain)))`
     - `selectedDomain`: current state
     - `onSelect`: `setSelectedDomain`
     - `itemCounts`: computed object mapping domain to count
   - Import and render `KnowledgeItemCard` for each item in the items array
   - Implement `handleDelete` function that:
     - Calls `fetch('/api/library/delete', { method: 'DELETE', headers: {'Content-Type':'application/json','Authorization':`Bearer ${token}`}, body: JSON.stringify({id}) })`
     - On success: refetch items with `fetchItems(selectedDomain ?? undefined)`
     - On error: alert("删除失败")
   - Implement `handleItemClick` that opens a detail view — for Phase 2, use a simple approach: either open a modal or navigate to `/library/${item.id}` (create the [id] route if navigating)

5. For the detail view, either:
   - Option A (Modal): Add `selectedItem: KnowledgeItem | null` state, render a modal when set
   - Option B (Route): Create `src/app/library/[id]/page.tsx` that fetches single item (requires new API route)

   **Recommendation**: Use Option A (Modal) for Phase 2 to avoid adding another API route. The modal shows full `item.content` (not just preview).

6. Add modal implementation in KnowledgeLibrary:
   - State: `viewingItem: KnowledgeItem | null`
   - When `viewingItem` is set, render a modal overlay:
     - Backdrop: `div className="fixed inset-0 bg-black/50 z-50 flex items-center justify-center"`
     - Content: `div className="bg-white rounded-lg max-w-2xl w-full mx-4 p-6 max-h-[80vh] overflow-y-auto"`
     - Show: title (h2), domain badge, full content (preserve newlines with `white-space: pre-wrap`), source link if present, created date, next review date
     - Close button: top-right X or "关闭" button at bottom

Import paths:
```typescript
"use client";
import { KnowledgeLibrary } from "@/components/library/KnowledgeLibrary";
import { useAuth } from "@/components/auth/AuthProvider";
import Link from "next/link";
```
  </action>
  <verify>
    <automated>cd E:/projects_all/voice-to-text/voice-to-txt-02 && npm run lint -- --max-warnings=0 2>&1 | grep -E "(error|warning)" | grep "library/page" || echo "lint OK"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/app/library/page.tsx
    - File starts with `"use client"`
    - File imports and renders KnowledgeLibrary
    - File contains auth guard (loading / not logged in states)
    - KnowledgeLibrary.tsx contains DomainSidebar import and rendering
    - KnowledgeLibrary.tsx contains KnowledgeItemCard import and rendering
    - KnowledgeLibrary.tsx contains handleDelete implementation with DELETE fetch
    - KnowledgeLibrary.tsx contains modal for viewing full item details
    - Modal shows full content with `white-space: pre-wrap` or equivalent
  </acceptance_criteria>
  <done>Library page exists, all components wired together, delete works, detail modal shows full content</done>
</task>

</tasks>

<verification>
Navigate to /library (requires logged-in user with items). Verify:
1. Sidebar shows domains with counts
2. Clicking domain filters items
3. List/grid toggle works
4. Clicking item opens modal with full content
5. Delete with confirmation removes item
</verification>

<success_criteria>
- /library page renders with sidebar navigation
- Domain filtering works
- List/grid view toggle works
- Item cards show title, domain, next review date, content preview
- Clicking item opens detail modal with full content
- Delete action works with confirmation
- All files pass npm run lint
</success_criteria>

<output>
After completion, create `.planning/phases/02-review-loop/02-02-SUMMARY.md`
</output>
